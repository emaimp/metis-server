services:
  searxng:
    image: searxng/searxng:latest
    container_name: metis-searxng
    ports:
      - "8888:8080"
    volumes:
      - ./docker/searxng:/etc/searxng:rw
    environment:
      - SEARXNG_BASE_URL=http://localhost:8888/
    restart: unless-stopped
